import React from 'react';

// import { Container } from './styles';

export default function FilmeCard({
  Title, Year, Released, Runtime, Genre, Director, Writer, Actors, Plot, Language, Country, Awards, Poster, Ratings, imdbRating,
}) {
  return (
    <div />
  );
}
